#include <bits/stdc++.h>
using namespace std;
#define __max(a, b) (((a) > (b)) ? (a) : (b))
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        long long n;
        cin >> n;
        cout << n / 2 << endl;
    }
    return 0;
}